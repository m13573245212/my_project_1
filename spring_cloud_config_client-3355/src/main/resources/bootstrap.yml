spring:
  cloud:
    config:
      name: config-client # 需要从GitHub上读取的资源名称,不需要后缀yml
      profile: dev # 本次访问的配置项
      label: master
      uri: http://spring-cloud-config3344.com:3344 # 先启动3344号服务,再通过本服务访问3344
